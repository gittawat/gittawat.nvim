gittawat's neovim setup

installation:
1. clone this repo
2. ln -s {absolute_path of the cloned repo} $HOME/.config/nvim


future plan...
1. adding bufferline or if possible make neovim-tree filter and show only openned buffer

